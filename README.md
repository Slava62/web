# Auto-tests web UI in stack Serenity + cucumber + JUnit5 + Java8

## Run tests:

`mvn clean verify`

## Look through the serenity-report's generated:

`mvn serenity:reports -Dserenity.reports=single-page-html`
